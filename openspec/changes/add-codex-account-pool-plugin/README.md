# add-codex-account-pool-plugin

Add a quota-aware Codex account pool scheduler plugin with strict priority tiers, weighted routing, backup accounts, scheduled quota refresh, and a visual management UI.
